"""Planner agent implementation."""

from ecomscout_ai.state.agent_state import AgentState


def planner_agent(state: AgentState) -> dict:
    """Planner node placeholder for the phase-1 linear workflow."""
    _ = state["user_query"]
    return {}
